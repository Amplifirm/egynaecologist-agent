# Supabase (server-side only — uses service role, NEVER expose to client)
SUPABASE_URL=https://lexbjgupbbfdzdewszuw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Front-desk login. The shared password your team types into the login screen.
DASHBOARD_PASSWORD=

# Random 32+ char string for cookie signing (HMAC). Generate with `openssl rand -hex 32`.
DASHBOARD_SECRET=
